node_modules/
dist/
.env
.env.local
.DS_Store
*.log

# Gmail OAuth — never commit these
backend/CredentialsOAuthGmail.json
backend/token.json
backend/*.json

# Personal transaction data — keep the folder structure, not the data
backend/data/*.json
backend/data/pdfs/

# Bank statement PDFs — never commit
backend/*.pdf
*.pdf

# Python virtualenv and cache
backend/.venv/
.venv/
__pycache__/
*.pyc
*.pyo
